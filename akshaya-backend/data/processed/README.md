# Processed Data

This folder is generated by the ingestion pipeline.
It contains cleaned text, chunks, metadata, and ingestion reports generated from the raw documents.

Generated contents are ignored by Git and should be rebuilt locally.

**Regeneration Instructions:**
To regenerate the processed chunks from the extracted text, run:
```bash
python -m ingestion.chunk_documents
```
